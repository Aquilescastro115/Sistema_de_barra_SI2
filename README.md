Para poner en marcha los contendores, tienes que ir por cmd a la carpeta a la que clonaste
el repositorio en github desktop y poner

docker-compose up --build

Para que hagan las pruebas.

Cuando haya cambios solamente y no sea la primera vez usandolo solo:

docker-compose up

